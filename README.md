# TDD Backend

Written in Django

## Installation

Install Poetry:
`curl -sSL https://install.python-poetry.org | python3 -`

Install python requirements:
`poetry install`

Activate pre-commit:
`pre-commit install`
